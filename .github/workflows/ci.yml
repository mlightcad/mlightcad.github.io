name: CI

on:
  pull_request:
    branches: [master, main]

permissions:
  contents: read
  packages: read

concurrency:
  group: ci-${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm
      - name: Install dependencies
        env:
          GITHUB_TOKEN: ${{ secrets.GH_PACKAGES_TOKEN }}
        run: pnpm install --frozen-lockfile
      - run: pnpm build
        env:
          VITE_DWG_LICENSE_KEY: ${{ secrets.VITE_DWG_LICENSE_KEY }}
          VITE_PADDLE_ENV: ${{ secrets.VITE_PADDLE_ENV }}
          VITE_PADDLE_CLIENT_TOKEN: ${{ secrets.VITE_PADDLE_CLIENT_TOKEN }}
          VITE_PADDLE_PRICE_PERPETUAL: ${{ secrets.VITE_PADDLE_PRICE_PERPETUAL }}
          VITE_PADDLE_PRICE_ANNUAL: ${{ secrets.VITE_PADDLE_PRICE_ANNUAL }}
          VITE_SUPABASE_URL: ${{ secrets.VITE_SUPABASE_URL }}
          VITE_SUPABASE_PUBLISHABLE_KEY: ${{ secrets.VITE_SUPABASE_PUBLISHABLE_KEY }}
